require FileSystem as FS;
require Console;

private mod Program
{
    private class MyClass
    {
        private var test;

        public constructor(test)
        {
            this.test = test;
        }

        public method Print()
        {
            Console.Out(this.test, "\n");
        }

        public function Test()
        {
            Console.Out("Hello!\n");
        }
    }

    private function Main()
    {
        var myClass = new MyClass(15);

        myClass.Print();
    }
}